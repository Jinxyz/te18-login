extends layout

block content
  main.container.d-flex.justify-content-center.align-items-center.h-100.w-100.flex-column
    form.form-signin(action="/register", method="post")
      h1.text-center.pb-3 🤢 Register User below! 🤢
      .mb-3
        label.form-label.visually-hidden(for="username")
        input#username.form-control.text-center(placeholder="💎Username💎" type="text", name="username", value=username ? username : '', class=errors ? 'is-invalid' : '')
        if errors 
          .invalid-feedback Username is invalid or unavailable
      .mb-3
        label.form-label.visually-hidden(for="email")
        input#password.form-control.text-center(placeholder="📪Email📪" type="email", name="email" class=errors ? 'is-invalid' : '')
        if errors 
          .invalid-feedback Email is invalid or unavailable
      .mb-3
        label.form-label.visually-hidden(for="password")
        input#password.form-control.text-center(placeholder="🔒Password🔒" type="password", name="password" class=errors ? 'is-invalid' : '')
        if errors 
          .invalid-feedback Passwords do not match 
      .mb-3
        label.form-label.visually-hidden(for="password")
        input#password.form-control.text-center(placeholder="✅Confirm password✅" type="password", name="password" class=errors ? 'is-invalid' : '')
        if errors 
          .invalid-feedback Passwords do not match 

      button.w-100.btn.btn-primary(type="submit") 😉 Sign up 😉